import { render } from "node-sass";
import React, { Component } from "react";

export default class Setting exports Component {
    state = {}

    render(){
        return(
            <h1>Setting Screen</h1>
        )
    }
}